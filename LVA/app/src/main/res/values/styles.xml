<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/color_text_root</item>
        <item name="colorPrimaryDark">@color/color_text_root</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="otpViewStyle">@style/OtpWidget.OtpView</item>
    </style>

    <style name="ToolbarColoredBackArrow" parent="AppTheme">
        <item name="android:textColorSecondary">@color/color_text_root</item>
    </style>

    <style name="TabLayout_Theme" parent="@style/AppTheme">
        <item name="android:maxLines">1</item>
        <item name="android:textSize">@dimen/s16</item>
    </style>
</resources>

